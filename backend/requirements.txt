fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
SQLAlchemy>=2.0,<3.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.9,<1.0
Pillow>=10.3,<12.0
pypdfium2>=4.30,<5.0
openpyxl>=3.1,<4.0

# HEIC/HEIF photo decode (iPhone uploads). Wheels usually bundle libheif;
# if the native library fails to load on a given host, the route falls
# back to a clean 400 response (see routers/job_photos.py).
pillow-heif>=0.18,<1.0

bcrypt>=4.0.1,<5.0
python-jose[cryptography]>=3.3,<4.0

# Postgres driver (Render Postgres / Supabase / any PostgreSQL).
psycopg[binary]>=3.1,<4.0
